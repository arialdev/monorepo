{
  "name": "monoserver",
  "version": "0.0.1",
  "description": "server for a monorepo prove of concept",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "test": "mocha",
    "build": "mkdir ./dist && cp server.js ./dist/server.js && exit 0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^10.0.0",
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "@fastify/cors": "^8.1.0",
    "fastify": "^4.6.0"
  },
  "type": "module"
}